CREATE TABLE IF NOT EXISTS lab.deployment_notes (
    note_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    note_title VARCHAR(150) NOT NULL,
    note_body TEXT,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
